<form method="post" action="{{ path('admin_auteur_delete', {'id': auteur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ auteur.id) }}">
    <button class="btn btn-secondary bg-secondary text-center mt-2">Delete</button>
</form>
